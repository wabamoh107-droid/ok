# AGENTS.md

## 🔴 قاعدة الصوت — إجبارية

هاد المشروع عندو **صوت افتراضي واحد** سميتو `MY_VOICE`.

قبل ما تولّد أي أوديو (`generate_speech`)، سجّل `MY_VOICE` بـ `add_voice` بهاد المعطيات
**حرفيا**:

| المعطى | القيمة |
|---|---|
| `language` | `ar` |
| `voice_identity.index` | `3` |
| `voice_identity.gender` | `masculine` |
| `voice_identity.use_case` | `narration` |

**نص الأوديشن (انسخو بلا تبديل):**

```
الملف كان مقفول. أربع سنين وهو محطوط فرف الأرشيف، بلا ما يقلبو حد. حتى جا نهار واحد، ومكالمة وحدة قلبات كلشي. اللي كانو كيقلبو عليه ماشي بعيد. كان قريب بزاف. قريب لدرجة أنك ملي تعرف الحقيقة، غادي تعاود تسمع لهاد الحكاية من الأول.
```

**الوصف:** صوت رجولي، نبرة خشينة ومشوّقة — للملفات البوليسية والجرائم الحقيقية.
**العينة المرجعية:** `samples/MY_VOICE.mp3`

### قواعد
- استعمل `MY_VOICE` تلقائياً. **ماتسولش** المستخدم على الصوت.
- **ماتبدلش** `index` ولا `gender` ولا نص الأوديشن.
- **ماتقترحش** صوت آخر إلا إلا طلبو المستخدم صراحة بالاسم.
- النصوص الطوال: قسّمهم على جمل كاملة وولّد بنفس الـ `voice_id`.

### أصوات إضافية (بالطلب فقط)
| الاسم | المعطيات | الاستعمال |
|---|---|---|
| `CRIME_INVESTIGATOR` | `ar` / `1` / `feminine` / `narration` | شهادات وتحقيق |
| `NEWS_ANCHOR` | `ar` / `2` / `masculine` / `narration` | مقدمات إخبارية |

التفاصيل الكاملة فـ [`VOICES.md`](./VOICES.md).
